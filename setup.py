from setuptools import setup

setup (
  name='debugnn',
  version='0.0.1',
  description='collecting of function to better debug neural networks',
  py_modules=['debugnn'],
  package_dir={'': 'src'}
)
